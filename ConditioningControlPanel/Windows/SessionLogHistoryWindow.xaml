<Window x:Class="ConditioningControlPanel.SessionLogHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
        Title="{loc:Str dialog_session_history}"
        Width="560" Height="640"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="HistoryRowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#252540"/>
            <Setter Property="BorderBrush" Value="#3A3A58"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2F2F52"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource DarkerBgBrush}" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="3" CornerRadius="20" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Left" Text="{loc:Str dialog_session_history}"
                           Foreground="{DynamicResource PinkBrush}" FontSize="22" FontWeight="Bold"
                           VerticalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Right" x:Name="TxtCount"
                           Foreground="#808090" FontSize="12"
                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </DockPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <TextBlock x:Name="TxtEmpty"
                               Text="{loc:Str label_no_session_logs}"
                               Foreground="#707080" FontSize="14" FontStyle="Italic"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Margin="0,40" Visibility="Collapsed"/>
                    <ItemsControl x:Name="LogList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource HistoryRowButtonStyle}"
                                        Click="LogRow_Click"
                                        Tag="{Binding}"
                                        Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0"
                                                   Foreground="White" FontSize="15" FontWeight="Bold"
                                                   TextTrimming="CharacterEllipsis">
                                            <Run Text="{Binding Icon, Mode=OneTime}"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding Name, Mode=OneTime}"/>
                                        </TextBlock>
                                        <TextBlock Grid.Row="0" Grid.Column="1"
                                                   Text="{Binding StatusText}"
                                                   Foreground="{Binding StatusBrush}"
                                                   FontSize="11" FontWeight="Bold"
                                                   VerticalAlignment="Center"/>

                                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                                   Foreground="#808090" FontSize="12"
                                                   Margin="0,4,0,0">
                                            <Run Text="{Binding StartedText, Mode=OneTime}"/>
                                            <Run Text="  ·  "/>
                                            <Run Text="{Binding DurationText, Mode=OneTime}"/>
                                            <Run Text="  ·  "/>
                                            <Run Text="{Binding MediaText, Mode=OneTime}"/>
                                        </TextBlock>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ScrollViewer>

            <Button Grid.Row="2" Content="{loc:Str btn_close}" Click="BtnClose_Click"
                    Background="{DynamicResource PinkBrush}" Foreground="White"
                    BorderThickness="0" Padding="28,10" FontSize="14" FontWeight="Bold"
                    Cursor="Hand" HorizontalAlignment="Center" Margin="0,14,0,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
