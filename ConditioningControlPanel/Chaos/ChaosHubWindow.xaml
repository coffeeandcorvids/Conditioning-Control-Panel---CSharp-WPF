<Window x:Class="ConditioningControlPanel.ChaosHubWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
        Title="the Dollhouse"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="CanResize" WindowStartupLocation="CenterScreen"
        SizeToContent="Manual" Width="990" Height="930"
        MinWidth="760" MinHeight="600" FontFamily="Segoe UI">
    <shell:WindowChrome.WindowChrome>
        <!-- CaptionHeight=0: no system caption (we drag via the title bars); the 6px resize
             border gives real edge-resize + correct maximize-to-workarea on this transparent window. -->
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="Pink" Color="#FFE84393"/>
        <SolidColorBrush x:Key="Purple" Color="#FF8B5CF6"/>
        <SolidColorBrush x:Key="Bg" Color="#FF14122A"/>
        <SolidColorBrush x:Key="Card" Color="#FF1C1A36"/>
        <SolidColorBrush x:Key="Card2" Color="#FF221F40"/>
        <SolidColorBrush x:Key="Border1" Color="#44E84393"/>
        <SolidColorBrush x:Key="TextDim" Color="#AAB8B8D0"/>
        <SolidColorBrush x:Key="TextMut" Color="#88A0A0C0"/>
        <LinearGradientBrush x:Key="Brand" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFE84393" Offset="0"/>
            <GradientStop Color="#FF8B5CF6" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="SectionHdr" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Pink}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="Lbl" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="92"/>
        </Style>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Card}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style x:Key="Seg" TargetType="ToggleButton">
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="b" CornerRadius="8" Background="#22FFFFFF" BorderBrush="#33FFFFFF" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Pink}"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Pink}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- tab pill -->
        <Style x:Key="Tab" TargetType="ToggleButton">
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="b" CornerRadius="9" Background="Transparent" BorderBrush="Transparent" BorderThickness="0,0,0,2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#22E84393"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Pink}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Stepper" TargetType="Button">
            <Setter Property="Width" Value="26"/><Setter Property="Height" Value="26"/>
            <Setter Property="Background" Value="#22FFFFFF"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Cursor" Value="Hand"/><Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="Chk" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,3"/>
        </Style>
        <!-- big left-aligned main-menu list button -->
        <Style x:Key="MenuBtn" TargetType="Button">
            <Setter Property="Height" Value="58"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="19"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="22,0"/>
            <Setter Property="Background" Value="{StaticResource Card}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="10" Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource Border1}" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Card2}"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Pink}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- big, scaled-up checkbox for the full-width OPTIONS panel -->
        <Style x:Key="OptChk" TargetType="CheckBox" BasedOn="{StaticResource Chk}">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Margin" Value="0,14"/>
            <Setter Property="LayoutTransform">
                <Setter.Value><ScaleTransform ScaleX="1.35" ScaleY="1.35"/></Setter.Value>
            </Setter>
        </Style>
        <!-- min / fullscreen / close window-control glyph button -->
        <Style x:Key="WinBtn" TargetType="Button">
            <Setter Property="Width" Value="30"/><Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="#22FFFFFF"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="{StaticResource Bg}" BorderBrush="{StaticResource Pink}" BorderThickness="2">
        <Grid>
        <!-- Full-width top drag strip. First child = behind everything, so it never blocks a
             button: it only catches drags in the transparent top gaps. The view title bars +
             WinChrome render on top of it. -->
        <Border x:Name="DragBar" Height="46" VerticalAlignment="Top" Background="Transparent"/>

        <!-- THE DOLL HOUSE: the tabbed hub. Hidden until chosen from the main menu. -->
        <Grid x:Name="DollhouseView" Margin="22" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- top bar -->
            <Grid Grid.Row="0" x:Name="TitleBar" Background="Transparent" Margin="0,0,0,14">
                <!-- Right-aligned decoration: Uniform keeps the art's full height visible at any
                     window width (UniformToFill was eating the subject's bottom on wide bars);
                     MaxHeight still guards the auto-sized title row against tall files. -->
                <Image x:Name="BannerImage" Stretch="Uniform" HorizontalAlignment="Right"
                       Opacity="0.30" Visibility="Collapsed" MaxHeight="120"/>
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Button x:Name="BtnBackToMenu" Content="‹ Menu" Click="Back_To_Menu_Click"
                            HorizontalAlignment="Left" Margin="0,0,0,8" Padding="10,3"
                            Background="#22FFFFFF" Foreground="{StaticResource TextDim}" FontSize="12"
                            BorderThickness="0" Cursor="Hand" ToolTip="back to the main menu">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="7"/></Style></Button.Resources>
                    </Button>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🐇 DOWN THE RABBIT HOLE" Foreground="White" FontSize="22" FontWeight="Bold">
                            <TextBlock.Effect><DropShadowEffect Color="#FFE84393" BlurRadius="20" ShadowDepth="0" Opacity="0.7"/></TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Text="the dollhouse" Foreground="{StaticResource Pink}" FontFamily="Consolas" FontWeight="Bold" FontSize="12" VerticalAlignment="Bottom" Margin="10,0,0,4"/>
                    </StackPanel>
                    <!-- rank / drops / gold under the title, left side — the banner art owns the right -->
                    <StackPanel Orientation="Horizontal" Margin="2,10,0,0">
                        <Border Background="{StaticResource Card2}" CornerRadius="9" Padding="12,5" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="RANK" Foreground="{StaticResource TextMut}" FontFamily="Consolas" FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock x:Name="TxtRank" Text="Curious" Foreground="White" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource Card2}" CornerRadius="9" Padding="12,5" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✦" Foreground="{StaticResource Purple}" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock x:Name="TxtSparks" Text="0" Foreground="White" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                                <TextBlock Text="drops" Foreground="{StaticResource TextMut}" FontSize="11" VerticalAlignment="Bottom" Margin="5,0,0,1"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource Card2}" CornerRadius="9" Padding="12,5" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🪙" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock x:Name="TxtGold" Text="0" Foreground="White" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                                <TextBlock Text="gold" Foreground="{StaticResource TextMut}" FontSize="11" VerticalAlignment="Bottom" Margin="5,0,0,1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
                <!-- Close/minimize/fullscreen live in the shared WinChrome cluster (top-right of the
                     root grid); the guide button sits left of it so they don't overlap. -->
                <Button x:Name="BtnGuide" Content="?" Click="BtnGuide_Click"
                        ToolTip="How to play — the rules card"
                        HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,120,0"
                        Width="28" Height="28" Background="#22FFFFFF" Foreground="White"
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </Grid>

            <!-- tabs: BAG (glance: pockets + collections), the Toybox (toy/accessory/habit shop),
                 Settings (run setup), the Looking Glass (stats/diary/mantras/bench) -->
            <WrapPanel Grid.Row="1" Margin="0,0,0,14">
                <ToggleButton x:Name="TabLoadout" Style="{StaticResource Tab}" Content="BAG" Tag="loadout" Click="Tab_Click"/>
                <ToggleButton x:Name="TabEnhance" Style="{StaticResource Tab}" Content="the Toybox" Tag="enhance" Click="Tab_Click"/>
                <ToggleButton x:Name="TabRun" Style="{StaticResource Tab}" Content="Settings" Tag="run" Click="Tab_Click"/>
                <ToggleButton x:Name="TabImprove" Style="{StaticResource Tab}" Content="the Looking Glass" Tag="improve" Click="Tab_Click"/>
                <ToggleButton x:Name="TabDiary" Style="{StaticResource Tab}" Content="the Diary" Tag="diary" Click="Tab_Click" Visibility="Collapsed"/>
            </WrapPanel>

            <!-- content -->
            <Grid Grid.Row="2">

                <!-- Faint dressing flourish behind every tab (assets/Chaos/hub/backdrop.png,
                     loaded in LoadBanner; absent file = collapsed, the void returns). -->
                <Image x:Name="HubBackdrop" Stretch="Uniform" MaxWidth="430" MaxHeight="430"
                       HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,4"
                       Opacity="0.12" IsHitTestVisible="False" Visibility="Collapsed"/>

                <!-- BAG: the glance page (pockets up top, then the whole collection as tiles) -->
                <ScrollViewer x:Name="PanelLoadout" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel>
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="POCKETS" Style="{StaticResource SectionHdr}" Margin="0,0,0,2"/>
                                <TextBlock x:Name="TxtPocketsSub" Text="what you take down with you. choose like it matters."
                                           Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,0,0,10"/>
                                <StackPanel x:Name="PocketSlotsHost" Orientation="Horizontal"/>
                            </StackPanel>
                        </Border>
                        <Border x:Name="CardBagAccessories" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <TextBlock Text="ACCESSORIES" Style="{StaticResource SectionHdr}" Margin="0"/>
                                    <TextBlock x:Name="TxtAccCount" Foreground="{StaticResource TextMut}" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                                <UniformGrid x:Name="TilesAccessories" Columns="4" HorizontalAlignment="Stretch"/>
                            </StackPanel>
                        </Border>
                        <Border x:Name="CardBagToys" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <TextBlock Text="TOYS" Style="{StaticResource SectionHdr}" Margin="0"/>
                                    <TextBlock x:Name="TxtSkillCount" Foreground="{StaticResource TextMut}" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                                <UniformGrid x:Name="TilesSkills" Columns="4" HorizontalAlignment="Stretch"/>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <Grid Margin="0,0,0,2">
                                    <TextBlock Text="HABITS" Style="{StaticResource SectionHdr}" Margin="0"/>
                                    <TextBlock x:Name="TxtHabitCount" Foreground="{StaticResource TextMut}" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                                <TextBlock Text="trained habits follow you down. tap one to switch it on or off."
                                           Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,0,0,10"/>
                                <UniformGrid x:Name="TilesHabits" Columns="4" HorizontalAlignment="Stretch"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- THE TOYBOX: unlock + deepen toys, accessories and habits -->
                <ScrollViewer x:Name="PanelEnhance" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="the Toybox" Style="{StaticResource SectionHdr}" FontSize="20" Margin="2,0,0,2"/>
                        <TextBlock Text="everything ordered here comes in an anonymous sealed package ;)"
                                   Foreground="{StaticResource TextMut}" FontSize="11" Margin="2,0,0,14"/>
                        <Border x:Name="HerCornerCard" Style="{StaticResource CardStyle}" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="her corner" Style="{StaticResource SectionHdr}" Margin="0,0,0,2"/>
                                <TextBlock Text="a little table by the door. she takes gold."
                                           Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,0,0,10"/>
                                <StackPanel x:Name="HerCornerHost"/>
                            </StackPanel>
                        </Border>
                        <TextBlock x:Name="HdrToys" Text="Toys" Style="{StaticResource SectionHdr}" FontSize="20" Margin="2,0,0,10"/>
                        <StackPanel x:Name="BoonHostSkills"/>
                        <Border x:Name="StubToys" Visibility="Collapsed"
                                Background="{StaticResource Card2}" BorderThickness="1" CornerRadius="8"
                                Padding="10" Margin="0,0,0,6" Opacity="0.55">
                            <Border.BorderBrush><SolidColorBrush Color="#1EE84393"/></Border.BorderBrush>
                            <TextBlock Text="???" Foreground="{StaticResource TextMut}" FontSize="12" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock x:Name="HdrAccessories" Text="Accessories" Style="{StaticResource SectionHdr}" FontSize="20" Margin="2,14,0,10"/>
                        <StackPanel x:Name="BoonHostAccessories"/>
                        <Border x:Name="StubAccessories" Visibility="Collapsed"
                                Background="{StaticResource Card2}" BorderThickness="1" CornerRadius="8"
                                Padding="10" Margin="0,8,0,6" Opacity="0.55">
                            <Border.BorderBrush><SolidColorBrush Color="#1EE84393"/></Border.BorderBrush>
                            <TextBlock Text="???" Foreground="{StaticResource TextMut}" FontSize="12" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="Habits" Style="{StaticResource SectionHdr}" FontSize="20" Margin="2,14,0,2"/>
                        <TextBlock Text="spend your drops. once trained they're yours for good. switch them on or off between descents."
                                   Foreground="{StaticResource TextMut}" FontSize="11" Margin="2,0,0,10"/>
                        <StackPanel x:Name="HabitsHost"/>
                    </StackPanel>
                </ScrollViewer>

                <!-- SETTINGS: run setup -->
                <ScrollViewer x:Name="PanelRun" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel>
                        <StackPanel x:Name="SetupBody">
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="THE DESCENT" Style="{StaticResource SectionHdr}" Margin="0,0,0,2"/>
                                <TextBlock Text="dress it up however you like. it ends the same way." Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="Difficulty" Style="{StaticResource Lbl}"/>
                                    <StackPanel x:Name="GrpDifficulty" Orientation="Horizontal">
                                        <ToggleButton Style="{StaticResource Seg}" Tag="Easy" Content="Gentle" Click="Segment_Click"/>
                                        <ToggleButton x:Name="SegMedium" Style="{StaticResource Seg}" Tag="Medium" Content="Teasing" Click="Segment_Click"/>
                                        <ToggleButton x:Name="SegHard" Style="{StaticResource Seg}" Tag="Hard" Content="Relentless" Click="Segment_Click"/>
                                        <ToggleButton x:Name="SegExtreme" Style="{StaticResource Seg}" Tag="Extreme" Content="Inescapable 🔒" Click="Segment_Click"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="Length" Style="{StaticResource Lbl}"/>
                                    <StackPanel x:Name="GrpLength" Orientation="Horizontal">
                                        <ToggleButton Style="{StaticResource Seg}" Tag="120" Content="2 min" Click="Segment_Click"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="180" Content="3 min" Click="Segment_Click"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="300" Content="5 min" Click="Segment_Click"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="Motion" Style="{StaticResource Lbl}"/>
                                    <StackPanel x:Name="GrpMotion" Orientation="Horizontal">
                                        <ToggleButton Style="{StaticResource Seg}" Tag="Mixed" Content="Mixed" Click="Segment_Click"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="FloatUp" Content="Float" Click="Segment_Click"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="RainDown" Content="Rain" Click="Segment_Click"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="RoamBounce" Content="Roam" Click="Segment_Click"/>
                                    </StackPanel>
                                </StackPanel>
                                <!-- Resistance stepper removed 2026-06-10: you descend with 0 —
                                     resistance is TRAINED ("It would never work on me..." charm). -->
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="SENSATION" Style="{StaticResource SectionHdr}"/>
                                <CheckBox x:Name="ChkShake" Style="{StaticResource Chk}" Content="Screen shake"/>
                                <Slider x:Name="SldShake" Minimum="0" Maximum="1" Margin="0,0,0,6"/>
                                <CheckBox x:Name="ChkFlashes" Style="{StaticResource Chk}" Content="Color flashes"/>
                                <CheckBox x:Name="ChkSkiaFx" Style="{StaticResource Chk}" Content="Enhanced FX (bloom &amp; bursts)"
                                          ToolTip="Additive Skia-rendered glow: rabbit trail, cursor halo, lightning, bubble pop bursts &amp; glassy shine. Off = the classic effects."/>
                                <CheckBox x:Name="ChkPinTop" Style="{StaticResource Chk}" Content="Keep on top"
                                          ToolTip="Pins the chaos layer above other windows. Off = other apps can come forward over the descent."/>
                                <CheckBox x:Name="ChkSharedHost" Style="{StaticResource Chk}" Content="Smooth bubbles (experimental)"
                                          ToolTip="Renders all bubbles on one shared layer instead of a window each, so clicks stay responsive when the screen is busy. Experimental — turn off if bubbles misbehave."/>
                                <TextBlock Text="Effect intensity" Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,6,0,0"/>
                                <Slider x:Name="SldEffect" Minimum="0.2" Maximum="1.5"/>
                                <CheckBox x:Name="ChkAnnouncer" Style="{StaticResource Chk}" Content="Announcements" Margin="0,8,0,0"/>
                                <TextBlock Text="THE MADAM" Style="{StaticResource SectionHdr}" Margin="0,12,0,0"/>
                                <CheckBox x:Name="ChkNarrative" Style="{StaticResource Chk}" Content="Narrator (voiced reactions)"
                                          ToolTip="A reactive narrator speaks voiced + text lines during a descent."/>
                                <CheckBox x:Name="ChkBackdrop" Style="{StaticResource Chk}" Content="Zone backdrops" Margin="0,4,0,0"
                                          ToolTip="Per-zone backdrop plates under the bubbles. Off = classic Chaos over your live desktop."/>
                                <TextBlock Text="Backdrop opacity" Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,6,0,0"/>
                                <Slider x:Name="SldBackdropOpacity" Minimum="0" Maximum="1"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="CardKeybinds" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="KEYBINDS" Style="{StaticResource SectionHdr}" Margin="0,0,0,2"/>
                                <TextBlock x:Name="TxtKeybindsSub" Text="your equipped toy fires on this, mid-descent. the second pocket isn't sewn yet."
                                           Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,0,0,10"/>
                                <StackPanel x:Name="RowToyKey1" Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="Toy 1" Style="{StaticResource Lbl}"/>
                                    <ComboBox x:Name="CmbAccKey1" Width="70" SelectionChanged="AccKey_Changed"/>
                                </StackPanel>
                                <StackPanel x:Name="RowToyKey2" Orientation="Horizontal">
                                    <TextBlock Text="Toy 2" Style="{StaticResource Lbl}"/>
                                    <ComboBox x:Name="CmbAccKey2" Width="70" SelectionChanged="AccKey_Changed"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- TESTING OPTIONS: dev/test knobs the descent normally tunes itself
                             (bubble pool, mantra/sin toggles, loop count). Collapsed by default
                             so the casual read is just difficulty + length + feel. -->
                        <ToggleButton x:Name="TglTesting" Style="{StaticResource Seg}" Content="🧪 testing options ▸"
                                      Click="TestingToggle_Click" HorizontalAlignment="Left" Margin="0,0,0,10"/>
                        <StackPanel x:Name="TestingBody" Visibility="Collapsed">

                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="BUBBLE POOL — what can drop" Style="{StaticResource SectionHdr}"/>
                                    <WrapPanel x:Name="GrpPool">
                                        <ToggleButton Style="{StaticResource Seg}" Tag="flash" Content="Flash" Margin="0,0,6,6"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="subliminal" Content="Subliminal" Margin="0,0,6,6"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="pink" Content="Pink Filter" Margin="0,0,6,6"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="spiral" Content="Spiral" Margin="0,0,6,6"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="braindrain" Content="BrainDrain" Margin="0,0,6,6"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="bambifreeze" Content="Bambi Freeze" Margin="0,0,6,6"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="video" Content="Video" Margin="0,0,6,6"/>
                                        <ToggleButton Style="{StaticResource Seg}" Tag="htlink" Content="Gif Rain" Margin="0,0,6,6"/>
                                    </WrapPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="presets:" Foreground="{StaticResource TextMut}" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <Button Content="Balanced" Tag="Balanced" Click="Preset_Click" Style="{StaticResource Stepper}" Width="Auto" Padding="10,4" Margin="0,0,6,0"/>
                                        <Button Content="Tease" Tag="Tease" Click="Preset_Click" Style="{StaticResource Stepper}" Width="Auto" Padding="10,4" Margin="0,0,6,0"/>
                                        <Button Content="Flash-only" Tag="Flash-only" Click="Preset_Click" Style="{StaticResource Stepper}" Width="Auto" Padding="10,4"/>
                                    </StackPanel>
                                    <CheckBox x:Name="ChkDarters" Style="{StaticResource Chk}" Content="White rabbits (fast bonus catch targets)" Margin="0,12,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="MANTRAS &amp; LOOPS" Style="{StaticResource SectionHdr}"/>
                                    <CheckBox x:Name="ChkBoonDraft" Style="{StaticResource Chk}" Content="Mantra draft between loops"/>
                                    <CheckBox x:Name="ChkCurses" Style="{StaticResource Chk}" Content="Allow sins"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="Loops" Foreground="{StaticResource TextDim}" FontSize="12" VerticalAlignment="Center" Width="50"/>
                                        <Button Style="{StaticResource Stepper}" Content="−" Tag="waves-" Click="Stepper_Click"/>
                                        <TextBlock x:Name="TxtWaves" Foreground="White" FontWeight="Bold" FontSize="14" Width="34" TextAlignment="Center" VerticalAlignment="Center"/>
                                        <Button Style="{StaticResource Stepper}" Content="+" Tag="waves+" Click="Stepper_Click"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <Button Content="↻ Randomize" Click="BtnRandomize_Click" Style="{StaticResource Stepper}" Width="Auto" Padding="12,8" Margin="0,0,8,0"/>
                                <Button Content="Defaults" Click="BtnDefaults_Click" Style="{StaticResource Stepper}" Width="Auto" Padding="12,8"/>
                            </StackPanel>
                        </StackPanel><!-- /TestingBody -->
                        </StackPanel><!-- /SetupBody -->
                    </StackPanel>
                </ScrollViewer>

                <!-- THE LOOKING GLASS: the seamstress's bench (meta unlocks) + mantras + stats & diary -->
                <Grid x:Name="PanelImprove" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="the seamstress's bench" Style="{StaticResource SectionHdr}" Margin="0,0,0,2"/>
                            <TextBlock Text="permanent alterations to the dollhouse itself. extra pockets, deeper doors."
                                       Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,0,0,10"/>
                            <ScrollViewer MaxHeight="190" VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="ImprovementsHost"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="CardMantras" Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,6,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="MANTRAS" Style="{StaticResource SectionHdr}" Margin="0,0,0,2"/>
                                <TextBlock Grid.Row="1" Text="click one to whisper it on the way down. click again for silence."
                                           Foreground="{StaticResource TextMut}" FontSize="11" Margin="0,0,0,8" TextWrapping="Wrap"/>
                                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="MantrasHost"/>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="6,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock x:Name="HdrStats" Grid.Row="0" Text="HOW FAR YOU'VE FALLEN" Style="{StaticResource SectionHdr}"/>
                                <Grid Grid.Row="1" x:Name="StatsGrid" VerticalAlignment="Top" Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Drops" Foreground="{StaticResource TextDim}" FontSize="15" Margin="0,4" Width="175"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="StSparks" Foreground="White" FontWeight="Bold" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Descents completed" Foreground="{StaticResource TextDim}" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="StRuns" Foreground="White" FontWeight="Bold" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Time under" Foreground="{StaticResource TextDim}" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="StTimeUnder" Foreground="White" FontWeight="Bold" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Best score" Foreground="{StaticResource TextDim}" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="StBestScore" Foreground="White" FontWeight="Bold" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Best streak" Foreground="{StaticResource TextDim}" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="4" Grid.Column="1" x:Name="StBestCombo" Foreground="White" FontWeight="Bold" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Total snapped" Foreground="{StaticResource TextDim}" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="5" Grid.Column="1" x:Name="StDefused" Foreground="White" FontWeight="Bold" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Time holding on" Foreground="{StaticResource TextDim}" FontSize="15" Margin="0,4"/>
                                    <TextBlock Grid.Row="6" Grid.Column="1" x:Name="StTimeHeld" Foreground="White" FontWeight="Bold" FontSize="15" Margin="0,4"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>

                <!-- the Diary: its own tab so a full stats sheet can't crowd it out.
                     Revealed only after the player has met something down there. -->
                <ScrollViewer x:Name="PanelDiary" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock x:Name="HdrDiary" Text="DIARY · what you've met down there · click to pop out ⤢"
                                       Cursor="Hand" MouseLeftButtonDown="Diary_PopOut" Foreground="{StaticResource Pink}"
                                       FontFamily="Consolas" FontWeight="Bold" FontSize="12" Margin="0,0,0,10"/>
                            <StackPanel x:Name="DiaryHost"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </Grid>

            <!-- footer -->
            <Grid Grid.Row="3" Margin="0,12,0,0">
                <TextBlock x:Name="TxtHint" Foreground="{StaticResource TextMut}" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- The play-mode pick moved to the Lab card (ChaosModeService.SelectedPlayMode);
                         this separate Free Desktop button is retired. Kept (collapsed) so it can be
                         restored if the pick ever returns to the hub. -->
                    <Button x:Name="BtnBeginDesktop" Content="🖥 FREE DESKTOP" Tag="FreeDesktop" Click="BtnBegin_Click"
                            Visibility="Collapsed"
                            ToolTip="Pop bubbles over your real desktop — no story or backdrop, and other windows can stay in front. Your companion floats along."
                            Background="#332A2A4A" Foreground="{StaticResource TextMut}" FontWeight="SemiBold" FontSize="14"
                            BorderBrush="#55E84393" BorderThickness="1" Padding="22,12" Margin="0,0,12,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border"><Setter Property="CornerRadius" Value="14"/></Style>
                        </Button.Resources>
                    </Button>
                    <!-- Single launcher. Mode is decided on the Lab card, so this is mode-neutral
                         ("FALL IN — STORY" label + Story tag removed). StartRun resolves the actual
                         mode from ChaosModeService.SelectedPlayMode. -->
                    <Button x:Name="BtnBegin" Content="▶ FALL IN" Click="BtnBegin_Click"
                            ToolTip="Begin the descent."
                            Background="{StaticResource Brand}" Foreground="White" FontWeight="Bold" FontSize="16" BorderThickness="0" Padding="32,13" Cursor="Hand">
                        <Button.Effect><DropShadowEffect Color="#FFE84393" BlurRadius="18" ShadowDepth="0" Opacity="0.6"/></Button.Effect>
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="14"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
        <!-- ============ MAIN MENU (cinematic split: menu left, art right) ============ -->
        <Grid x:Name="MenuView" Margin="22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- right: character / banner art fills the right half. Thick pink neon border with
                 rounded corners + an animated glow (pulsed in code-behind SetupMenuMotion). -->
            <Border x:Name="MenuArtPanel" Grid.Column="1" CornerRadius="24" Margin="18,52,2,2"
                    BorderThickness="3">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFF5FB0" Offset="0"/>
                        <GradientStop Color="#FFE84393" Offset="0.5"/>
                        <GradientStop Color="#FF9B6CF6" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <Border.Effect>
                    <DropShadowEffect Color="#FFE84393" ShadowDepth="0" BlurRadius="20" Opacity="0.7"/>
                </Border.Effect>
                <Grid x:Name="MenuArtClip" Background="Transparent" MouseLeftButtonDown="MenuArt_Click">
                    <!-- Every layer rounds itself: a Border clips its own Background/ImageBrush to its
                         CornerRadius (reliable, unlike UIElement.Clip here). Fog rounds at the canvas. -->
                    <Border CornerRadius="22">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#22E84393" Offset="0"/>
                                <GradientStop Color="#11000000" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    <!-- two stacked image layers (rounded image-brush borders) so frames can crossfade;
                         both share the breathing motion via the wrapping MenuArtMotion grid -->
                    <Grid x:Name="MenuArtMotion" RenderTransformOrigin="0.5,0.5">
                        <Border CornerRadius="22">
                            <Border.Background><ImageBrush x:Name="MenuArtBrush" Stretch="UniformToFill"/></Border.Background>
                        </Border>
                        <Border x:Name="MenuArtTopBox" CornerRadius="22" Opacity="0">
                            <Border.Background><ImageBrush x:Name="MenuArtTopBrush" Stretch="UniformToFill"/></Border.Background>
                        </Border>
                    </Grid>
                    <!-- animated pink fog drifting IN FRONT of the character (Skia, additive-soft).
                         Rounded at the canvas; gated on the Enhanced FX setting. -->
                    <skia:SKElement x:Name="MenuFog" IsHitTestVisible="False" PaintSurface="MenuFog_PaintSurface"/>
                </Grid>
            </Border>

            <!-- left: title + menu + chips -->
            <Grid x:Name="MenuLeftCol" Grid.Column="0" Width="370">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- title (drag region) — neon logo wordmark, centered above the buttons -->
                <StackPanel Grid.Row="0" x:Name="MenuTitleBar" Background="Transparent">
                    <!-- neon wordmark rendered in Skia so the authored glint FX is true additive
                         light on the logo's real pixels — transparent surface, no box around it -->
                    <skia:SKElement x:Name="MenuLogoFx" IsHitTestVisible="False"
                                    Width="314" Height="224" HorizontalAlignment="Center"
                                    Margin="0,48,0,0" PaintSurface="MenuLogoFx_PaintSurface"/>
                    <TextBlock Text="· the dollhouse ·" Foreground="{StaticResource Pink}" FontFamily="Consolas"
                               FontWeight="Bold" FontSize="12" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- center: the menu list (options now lives full-width — see MenuOptions below) -->
                <Grid Grid.Row="1" VerticalAlignment="Center" Margin="0,18,0,18">
                    <StackPanel x:Name="MenuButtons">
                        <Button x:Name="BtnMenuFallIn" Content="▶  FALL IN" Click="Menu_FallIn_Click"
                                Height="64" Margin="0,0,0,14" FontSize="22" FontWeight="Bold" Foreground="White"
                                Background="{StaticResource Brand}" BorderThickness="0" Cursor="Hand"
                                HorizontalContentAlignment="Left" Padding="22,0" ToolTip="you were always going to.">
                            <Button.Effect><DropShadowEffect Color="#FFE84393" BlurRadius="18" ShadowDepth="0" Opacity="0.6"/></Button.Effect>
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="12"/></Style></Button.Resources>
                        </Button>
                        <Button x:Name="BtnMenuHowTo" Style="{StaticResource MenuBtn}" Content="📖  HOW TO PLAY"
                                Click="Menu_HowTo_Click" ToolTip="new here? thirty seconds and you'll have it."/>
                        <Button x:Name="BtnMenuDollhouse" Style="{StaticResource MenuBtn}" Content="🏠  THE DOLL HOUSE"
                                Click="Menu_Dollhouse_Click" ToolTip="pockets, toys, the bench — dress the fall."/>
                        <Button x:Name="BtnMenuStory" Style="{StaticResource MenuBtn}" Content="📖  STORY MODE  🔒"
                                Click="Menu_Story_Click" ToolTip="the Madam's narrated descent — coming soon."/>
                        <Button x:Name="BtnMenuOptions" Style="{StaticResource MenuBtn}" Content="⚙  OPTIONS" Click="Menu_Options_Click"/>
                        <Button x:Name="BtnMenuExit" Style="{StaticResource MenuBtn}" Content="✕  EXIT" Click="Menu_Exit_Click"/>
                    </StackPanel>
                </Grid>

                <!-- bottom: rank / drops / gold -->
                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <Border Background="{StaticResource Card2}" CornerRadius="9" Padding="10,5" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="RANK" Foreground="{StaticResource TextMut}" FontFamily="Consolas" FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="MenuRank" Text="Curious" Foreground="White" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="{StaticResource Card2}" CornerRadius="9" Padding="10,5" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✦" Foreground="{StaticResource Purple}" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="MenuSparks" Text="0" Foreground="White" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="{StaticResource Card2}" CornerRadius="9" Padding="10,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🪙" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="MenuGold" Text="0" Foreground="White" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <!-- soundtrack mute toggle (icon swaps 🔊/🔇 in code-behind) -->
                    <Button x:Name="BtnMenuMute" Click="BtnMenuMute_Click" Cursor="Hand"
                            ToolTip="Mute / unmute the menu music" Background="{StaticResource Card2}"
                            BorderThickness="0" Padding="10,5" Margin="8,0,0,0">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="9"/></Style></Button.Resources>
                        <TextBlock x:Name="MenuMuteIcon" Text="🔊" FontSize="15" VerticalAlignment="Center"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- OPTIONS: full-width sub-screen (art + left column hide while it's up) so the toggles
                 use the whole window — bigger and spread across two columns. The overlapping ones
                 two-way bind to the Settings-tab controls so there's a single source of truth. -->
            <Border x:Name="MenuOptions" Grid.ColumnSpan="2" Visibility="Collapsed"
                    Background="{StaticResource Card}" BorderBrush="{StaticResource Border1}" BorderThickness="2"
                    CornerRadius="14" Padding="40,30">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="⚙  OPTIONS" Foreground="{StaticResource Pink}" FontFamily="Consolas" FontWeight="Bold" FontSize="28"/>
                        <TextBlock Text="display &amp; comfort — these stick between descents." Foreground="{StaticResource TextMut}" FontSize="13" Margin="2,6,0,0"/>
                    </StackPanel>

                    <Grid Grid.Row="1" Margin="0,12,0,0" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="64"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- left: toggles -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="6,0,0,0">
                            <CheckBox x:Name="OptFullscreen" Style="{StaticResource OptChk}" Content="Fullscreen" Click="OptFullscreen_Click"/>
                            <CheckBox x:Name="OptKeepTop" Style="{StaticResource OptChk}" Content="Keep on top"
                                      IsChecked="{Binding IsChecked, ElementName=ChkPinTop, Mode=TwoWay}"/>
                            <CheckBox x:Name="OptSkiaFx" Style="{StaticResource OptChk}" Content="Enhanced FX (bloom &amp; bursts)"
                                      IsChecked="{Binding IsChecked, ElementName=ChkSkiaFx, Mode=TwoWay}"/>
                            <CheckBox x:Name="OptAnnounce" Style="{StaticResource OptChk}" Content="Announcements"
                                      IsChecked="{Binding IsChecked, ElementName=ChkAnnouncer, Mode=TwoWay}"/>
                        </StackPanel>

                        <!-- right: intensity slider -->
                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,6,0">
                            <TextBlock Text="EFFECT INTENSITY" Foreground="{StaticResource TextMut}" FontFamily="Consolas" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Slider Minimum="0.2" Maximum="1.5" Value="{Binding Value, ElementName=SldEffect, Mode=TwoWay}" Height="30"/>
                            <TextBlock Text="lower = gentler bloom &amp; shake." Foreground="{StaticResource TextMut}" FontSize="12" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Grid>

                    <Button Grid.Row="2" Content="‹ back" Click="Options_Back_Click" Style="{StaticResource MenuBtn}"
                            HorizontalAlignment="Left" Width="170" Height="52" Margin="0,16,0,0"/>
                </Grid>
            </Border>

            <!-- HOW TO PLAY: card overlay floating over the menu (art keeps breathing, music keeps
                 playing underneath). Built one card at a time in code-behind (HowToShow). -->
            <Border x:Name="MenuHowTo" Grid.ColumnSpan="2" Visibility="Collapsed" Background="#D80B0B16"
                    MouseLeftButtonDown="HowTo_Backdrop_Click">
                <Border Width="660" MaxHeight="660" VerticalAlignment="Center" HorizontalAlignment="Center"
                        Background="{StaticResource Card}" BorderBrush="{StaticResource Border1}" BorderThickness="2"
                        CornerRadius="18" Margin="0,40,0,0">
                    <Border.Effect><DropShadowEffect Color="#FFE84393" ShadowDepth="0" BlurRadius="34" Opacity="0.45"/></Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- card image (hidden when no screenshot present) -->
                        <Border x:Name="HowToImageBox" Grid.Row="0" Height="252" CornerRadius="16,16,0,0" ClipToBounds="True">
                            <Grid>
                                <Border CornerRadius="16,16,0,0">
                                    <Border.Background><ImageBrush x:Name="HowToImageBrush" Stretch="UniformToFill"/></Border.Background>
                                </Border>
                                <Border CornerRadius="16,16,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                            <GradientStop Color="#FF1C1A36" Offset="0"/>
                                            <GradientStop Color="Transparent" Offset="0.85"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                        </Border>

                        <!-- title -->
                        <StackPanel Grid.Row="1" Margin="32,18,32,0">
                            <TextBlock x:Name="HowToStep" Text="STEP 1 / 5" Foreground="{StaticResource Pink}"
                                       FontFamily="Consolas" FontWeight="Bold" FontSize="11" Margin="0,0,0,4"/>
                            <TextBlock x:Name="HowToTitle" Text="" Foreground="White" FontSize="22" FontWeight="Bold" TextWrapping="Wrap"/>
                        </StackPanel>

                        <!-- body lines (built in code-behind) -->
                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="32,12,32,8">
                            <StackPanel x:Name="HowToBody"/>
                        </ScrollViewer>

                        <!-- footer: back · dots · next -->
                        <Grid Grid.Row="3" Margin="24,8,24,22">
                            <Button x:Name="HowToBack" Content="‹  BACK" Click="HowTo_Back_Click" Style="{StaticResource MenuBtn}"
                                    HorizontalAlignment="Left" Width="140" Height="46"/>
                            <StackPanel x:Name="HowToDots" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Button x:Name="HowToNext" Content="NEXT  ›" Click="HowTo_Next_Click"
                                    HorizontalAlignment="Right" Width="140" Height="46" FontWeight="Bold" FontSize="14"
                                    Foreground="White" Background="{StaticResource Brand}" BorderThickness="0" Cursor="Hand">
                                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style></Button.Resources>
                            </Button>
                        </Grid>

                        <!-- close X -->
                        <Button x:Name="HowToClose" Grid.Row="0" Content="✕" Click="HowTo_Close_Click"
                                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,12,0" Panel.ZIndex="5"
                                Width="34" Height="34" FontSize="14" Foreground="White" Cursor="Hand"
                                Background="#66000000" BorderThickness="0" ToolTip="Close">
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="17"/></Style></Button.Resources>
                        </Button>
                    </Grid>
                </Border>
            </Border>
        </Grid>

        <!-- unlock cards float top-center over everything; never blocks clicks (shopping continues under it) -->
        <Grid x:Name="UnlockCardLayer" IsHitTestVisible="False"/>

        <!-- shared window controls (top-right, above both views) -->
        <StackPanel x:Name="WinChrome" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,10,12,0" Panel.ZIndex="50">
            <Button x:Name="BtnMin" Style="{StaticResource WinBtn}" Content="—" Click="BtnMin_Click" ToolTip="Minimize"/>
            <Button x:Name="BtnFull" Style="{StaticResource WinBtn}" Content="⛶" Click="BtnFull_Click" ToolTip="Fullscreen (maximize)"/>
            <Button x:Name="BtnCloseWin" Style="{StaticResource WinBtn}" Content="✕" Click="BtnClose_Click" ToolTip="Close"/>
        </StackPanel>
        </Grid>
    </Border>
</Window>
